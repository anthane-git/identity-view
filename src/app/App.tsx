import { BrowserRouter } from 'react-router-dom';

import { Router } from '@router';

export const App = () => {
	return (
		<BrowserRouter>
			<h1>TEST OK</h1>
			<Router />
		</BrowserRouter>
	);
};
